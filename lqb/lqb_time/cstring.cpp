//字符串练习
#include<bits/stdc++.h>
using namespace std;
int main()
{
   /* char s[105];
    int len , cot = 0;
    scanf("%s",s);
    len = strlen(s);
    for(int i = 0;i < len;i++)
    {
        if(s[i] == 'A')
        {
            cot++;
        }
        
    }
    printf("%d",cot);
    */
   //cpp
   /*
   string st;
   cin>>st;
   int cot = 0;
   int len = st.size();
    for(int i = 0;i < len;i++)
    {
        if(st[i] == 'A')
        {
            cot++;
        }
    }
    cout<<cot;
    return 0;
    */
   
}
